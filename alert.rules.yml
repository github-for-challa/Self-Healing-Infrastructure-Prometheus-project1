groups:
  - name: nginx_alerts
    rules:
      - alert: NginxDown
        expr: probe_success == 0
        for: 30s
        labels:
          severity: critical
        annotations:
          summary: "NGINX down"
